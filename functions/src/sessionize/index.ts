/**
 * Speakers domain — re-exports every Cloud Function this domain owns.
 * Add a new export here when a new speaker-related function is created.
 */

export { refreshSpeakers } from './refresh-speakers.js';
