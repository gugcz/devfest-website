# Deploys Cloud Functions (codebase: website) on push to the release branch.
#
# No-delete guarantee: this workflow runs `firebase deploy` in
# `--non-interactive` mode WITHOUT `--force`. If a function exists in the
# project but no longer in the codebase, the CLI cannot prompt for deletion
# and the deploy fails loudly. To remove a function, delete it manually with
#   firebase functions:delete <name> --region europe-west1
# and then re-run this workflow.

name: Deploy Functions on merge
on:
  push:
    branches:
      - "2026"
    paths:
      - "functions/**"
      - "firebase.json"
      - ".github/workflows/firebase-functions-merge.yml"
  # Manual re-deploy, no code change needed. Firebase pins each secret to the
  # version resolved at deploy time, so a rotated secret (e.g. a new
  # RESEND_API_KEY version) only reaches the functions on the next deploy —
  # run this workflow from the Actions tab after adding the new version.
  workflow_dispatch:

permissions:
  contents: read

# Two merges to 2026 in quick succession must not race two overlapping
# deploys — queue them instead of cancelling either.
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  deploy_functions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - name: Install function deps
        working-directory: functions
        run: npm ci
      - name: Build functions
        working-directory: functions
        run: npm run build
      - name: Write service account credentials
        env:
          SA_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DEVFEST_CZ_APP }}
        run: |
          printf '%s' "$SA_JSON" > "$RUNNER_TEMP/sa.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/sa.json" >> "$GITHUB_ENV"
      - name: Write functions env (non-secret ti.to slugs)
        env:
          TITO_ACCOUNT_SLUG: ${{ vars.TITO_ACCOUNT_SLUG }}
          TITO_EVENT_SLUG: ${{ vars.TITO_EVENT_SLUG }}
        run: |
          if [ -z "$TITO_ACCOUNT_SLUG" ] || [ -z "$TITO_EVENT_SLUG" ]; then
            echo "::error::TITO_ACCOUNT_SLUG or TITO_EVENT_SLUG repo variable is unset — refusing to deploy with an empty ti.to slug" >&2
            exit 1
          fi
          {
            printf 'TITO_ACCOUNT_SLUG=%s\n' "$TITO_ACCOUNT_SLUG"
            printf 'TITO_EVENT_SLUG=%s\n' "$TITO_EVENT_SLUG"
          } > functions/.env
      - name: Deploy functions
        run: |
          npx --yes firebase-tools@15.30.0 deploy \
            --only functions:website \
            --project devfest-cz-app \
            --non-interactive
