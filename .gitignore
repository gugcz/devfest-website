# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
firebase-debug.log*
firestore-debug.log*

# firebase local artifacts
.firebase/


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code: worktrees and per-machine settings stay local;
# .claude/rules/ and .claude/settings.json are shared.
.claude/worktrees/
.claude/settings.local.json
CLAUDE.local.md

# impeccable: shared config, sidecar and surface briefs are committed;
# per-machine consent, caches and live-mode journals stay local.
.impeccable/*
!.impeccable/config.json
!.impeccable/design.json
!.impeccable/surfaces/
!.impeccable/live/
.impeccable/live/inject-journal.json
.agent_context/

# pencil canvas: fetched/rendered images the repo cannot serve (see scripts/design-assets.mjs)
design/assets/
